name = "amici-erp"
compatibility_date = "2026-06-16"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"

# VITE_* vars are embedded at build time by Vite — set them in .env or CI, not here.
# Example: VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY in your build environment.
